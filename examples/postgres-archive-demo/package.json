{
  "name": "postgres-archive-demo-example",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Round-trip a plastron State through a postgres-backed `.甲` archive — ensureSchema, saveArchive, listArchives, loadArchive, deleteArchive.",
  "scripts": {
    "start": "tsx src/index.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "~5.9.3"
  }
}
