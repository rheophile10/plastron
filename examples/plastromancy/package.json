{
  "name": "plastromancy-example",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Plastromancy SPA — divination as a reactive plastron app.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^5.4.10"
  }
}
