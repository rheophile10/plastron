{
  "name": "plastron-eshkol",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Eshkol kind handler for plastron — run Scheme/Lisp lambdas through the Eshkol bytecode VM (WASM).",
  "main": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
