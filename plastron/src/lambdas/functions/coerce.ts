export const toNumber  = ({ a }: { a: any }): number => Number(a);
export const toString  = ({ a }: { a: any }): string => String(a);
export const toBoolean = ({ a }: { a: any }): boolean => Boolean(a);
export const typeOf    = ({ a }: { a: any }): string => typeof a;
